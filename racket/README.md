(transform puzzle) is working as expected, however unfortunately had major trouble getting solve to function properly. Began working on a function to search the sets in each row and remove the known numbers (i.e sets containing just one number). It works for one pass, but doesn't keep the state.
